eureka:
  server:
    renewal-percent-threshold: 0.60
  instance:
    hostname: master
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://slave:8752/eureka/
spring:
  profiles: master
  application:
    name: eurka-server-master
