eureka:
  server:
    renewal-percent-threshold: 0.60
  instance:
    hostname: slave
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://master:8751/eureka/
spring:
  profiles: slave
